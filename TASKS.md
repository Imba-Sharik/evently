# Evently — Задачи

## В работе

## Бэклог

### Мероприятия — Страница мероприятия. Добавить виджет шаблонов

Блок с поиском и карточками шаблонов на странице мероприятия.

---

## Завершено

### [4 SP] Форма мероприятия — правки по дизайну

Приведение `EventFormPage` к макету + мелкие баги.

- [1 SP] Обновить лейблы полей: «Введите название», «Добавьте описание», «Кол-во мест», «Выберите локацию»
- [1 SP] Добавить заголовок формы с датой/временем + заглушку «Готовый шаблон» (disabled Select)
- [1 SP] Переместить поле локации вниз; добавить чекбокс «Сохранить как шаблон» (state-only); кнопку «Создать» вправо
- [1 SP] Баги: `md:text-lg` для Textarea, `position="popper"` для Select, `line-clamp-4` в EventCard, адаптив (`flex-wrap`, `flex-col sm:flex-row`)

---

### [2 SP] Таблица локаций — убрать «Создать мероприятие» из дропдауна

Удалён пункт «Создать мероприятие» из контекстного меню строки в `AdminLocationsTable`.

---

### [8 SP] Мероприятия — рефакторинг: модалка → отдельная страница

Перенос создания/редактирования мероприятий из диалога на отдельные страницы.

- [2 SP] Создать `EventFormPage` — клиентский компонент с формой (без Dialog-обёртки)
- [1 SP] Создать `/admin/events/new` — server component, загружает локации + опциональный шаблон по `?template=<id>`
- [1 SP] Создать `/admin/events/[id]/edit` — server component, загружает событие + локации
- [2 SP] Обновить `AdminEventsTable` — убрать диалог-стейт, кнопки теперь делают `router.push`
- [1 SP] Удалить папку `/admin/locations/[id]/events` (старая страница расписания)
- [1 SP] Обновить `PageTitle` — добавить маршруты `new` и `edit`

---

### [3 SP] Страница создания мероприятия — добавить превью и календарь

Двухколоночный лейаут: слева тёмный виджет календаря + предпросмотр `EventCard`, справа форма.

- [1 SP] Добавить `EventCard` с live-превью (обновляется при вводе)
- [1 SP] Встроить тёмный календарь (`bg-linear-to-br from-[#1F1F1F] to-[#666666]`) вместо Popover
- [1 SP] Перенести календарь из формы — над превью (в левую колонку)

---

### [1 SP] Страница создания — убрать выбор нескольких дат

Закомментирован toggle «+ Несколько дат», режим `mode="multiple"` и чипы дат. Календарь — только `mode="single"`.

---

### [1 SP] Страница создания — центрирование контента (`max-w-5xl mx-auto`)
